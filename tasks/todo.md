# anandiyer.com redesign — plan

**Goal:** Replace WordPress anandiyer.com with a clean, Collison-style single-page personal site on GitHub Pages. Highlights accomplishments, surfaces live writing (Substack) and tweets (X).

## Decisions locked in
- **Aesthetic:** Collison-style (serif Charter, single column, near-black on off-white, text-first). Distinct from canonical.cc brand family — this is Anand's page.
- **Hosting:** New GitHub repo `anand-site` (working name) → GitHub Pages → `CNAME` to `anandiyer.com`. Existing `anandiyer.github.io` stays as-is for canonical.cc.
- **No build step.** Plain HTML + CSS + tiny JS island. Static-with-cron pattern: GitHub Actions auto-refresh dynamic sections hourly.
- **Sections:** Hero · Now · Writing (Substack) · Talks · Tweets · FAQ · Contact. (Investments removed per Anand.)
- **Writing rail:** auto-fetched from Substack RSS, *inlined into HTML* (not JS-rendered) so essay titles land in initial page for SEO/GEO.
- **Tweet rail:** auto-fetched from `syndication.twitter.com`, JS-rendered (enrichment only, not entity-defining).

## File layout
```
anand-site/
├── index.html                       # all sections inline; writing block patched by cron
├── css/style.css                    # ~180 lines, no framework
├── js/render.js                     # tweet rail only (writing is inline HTML)
├── data/
│   ├── tweets.json                  # auto-refreshed hourly by GH Actions
│   ├── writing.json                 # auto-refreshed hourly (source of truth alongside HTML)
│   └── talks.json                   # manual
├── scripts/
│   ├── fetch-tweets.py              # Twitter syndication → tweets.json
│   ├── fetch-substack.py            # Substack RSS → writing.json + patches index.html
│   └── make-og.py                   # generates images/og.png
├── images/og.png                    # 1200×630 OG/Twitter card
├── .github/workflows/
│   ├── refresh-tweets.yml           # cron :17
│   └── refresh-writing.yml          # cron :23
├── CNAME                            # "anandiyer.com"
├── robots.txt                       # explicit allow for GPTBot/ClaudeBot/PerplexityBot
├── sitemap.xml
├── llms.txt                         # GEO — clean factual bio for LLM ingestion
└── README.md
```

## Status
- [x] Scaffold HTML/CSS/JS
- [x] Tighten spacing per Anand (sections 5→3rem, hero 4→2.5rem, padding 5/8→3.5/4rem)
- [x] Add Collison-style serif typography, off-white bg, single-column reading width
- [x] Tweet rail: live from `syndication.twitter.com` → `data/tweets.json` (auto, hourly)
- [x] Writing rail: live from Substack RSS → inline HTML patched by `scripts/fetch-substack.py`
- [x] Blog link to anandiyer.substack.com in top nav + footer
- [x] Remove investments section per Anand
- [x] **SEO/GEO P0:** JSON-LD Person + Organization + FAQPage schema, sameAs disambiguation, FAQ section
- [x] OG image (1200×630) — `images/og.png` generated by `scripts/make-og.py`
- [x] `robots.txt` (explicit allow for GPTBot, ClaudeBot, PerplexityBot, etc.)
- [x] `sitemap.xml`
- [x] `llms.txt`
- [x] Canonical, og:*, twitter:* meta tags

## Open — needs Anand's input
- [ ] **Talks/press** — placeholder entries; need real items (or remove section)
- [ ] **LinkedIn URL** — schema sameAs assumes `linkedin.com/in/anandiyer/`; confirm slug
- [ ] **Crunchbase URL** — schema sameAs assumes `crunchbase.com/person/anand-iyer`; confirm slug
- [ ] **Favicon** — currently reuses default; want a custom one?
- [ ] **Repo name** — `anand-site` or `anandiyer.com`?

## Open — Anand actions (off-site, ranking impact)
- [ ] Update LinkedIn website field → `anandiyer.com`
- [ ] Update Canonical's team page bio link → `anandiyer.com`
- [ ] Update Lightspeed Venture Partner bio page → `anandiyer.com`
- [ ] Crunchbase profile website field → `anandiyer.com`
- [ ] Submit `sitemap.xml` to Google Search Console (after cutover)
- [ ] Submit to Bing Webmaster Tools (powers ChatGPT search citations)

## Deploy steps (when ready)
1. [ ] `git init`, push to new GitHub repo `anandiyer/anand-site`
2. [ ] Enable GitHub Pages on `master`
3. [ ] Verify hourly cron workflows fire (`Actions` tab)
4. [ ] DNS swap at registrar:
       - A records → `185.199.108.153, .109.153, .110.153, .111.153`
       - `www` CNAME → `<github-user>.github.io`
5. [ ] Wait for GitHub Pages HTTPS provisioning (~10 min)
6. [ ] Verify OG image preview via [opengraph.dev](https://www.opengraph.dev/) or [metatags.io](https://metatags.io)
7. [ ] Decide: WordPress URL redirects? (Cloudflare Page Rules vs. catch-all 404)
8. [ ] Sunset WordPress hosting

## Out of scope (v1)
- Blog hosted here (link out to Substack)
- Multi-page
- Analytics (Plausible/GA can be added later)
- A11y audit (basic semantic HTML is already solid)

## Review

**What worked:**
- Static-with-cron is the right architecture for a personal site: page is fast and resilient (works even if Twitter/Substack are down — last-good JSON keeps serving), but content auto-refreshes hourly without manual upkeep.
- Inlining writing into HTML (vs JS-rendering) is the right call for SEO since essay titles are entity-relevant; tweets stay JS-rendered because they're enrichment.
- Twitter's `syndication.twitter.com` and Substack's `/feed` are both free, unauthenticated, and stable — same pattern as react-tweet uses.

**Notes for next iteration:**
- Talks/press section is still placeholder; consider removing if no real items to feature.
- Could add a small "Speaking" section (events Anand is speaking at) if relevant.
- Consider adding `last-modified` headers via CF page rule to help Google freshness signal.

---

# AEO readiness pass — 2026-08-16

Applied the [Canonical AEO Readiness Check](https://canonical.cc/labs/aeo/?d=anandiyer.com)
fix plan (scan of 2026-08-13, scored **57/100 · Grade C**).

## Done

- [x] **Task 2 — Service schema.** Added `Service` JSON-LD (`#investing`) describing
      pre-seed/seed investing. No `offers` block: a check size is not a published
      price, and the plan forbids inventing one. `provider` lists the Person first,
      then the Canonical Organization, so the node anchors to this domain.
- [x] **Task 3 — Question headings.** The section eyebrows were `<span class="label">`,
      so the scan saw *zero* headings. Converted to `<h2 class="label">` phrased as
      questions, and wrapped each FAQ `<summary>` in `<h3>`. Now 10 of 11 headings
      are questions (0% → 91%), with exactly one `<h1>`. `.label` out-specifies the
      bare `h2` rule, so rendering is byte-identical to before.
- [x] **Task 4 — Meta description.** Was 233 chars (outside the 50–200 window, which
      scores as absent). Rewrote to 178.
- [x] **Task 5 — Markdown twin.** Added `/index.md` + `<link rel="alternate"
      type="text/markdown">`. Added `.nojekyll` so Pages serves the `.md` verbatim
      instead of rendering it into a file that collides with `index.html`.
      `fetch-substack.py` now patches the writing block in *both* files, and the
      workflow commits both — a twin that drifts is worse than no twin.
- [x] **Task 6 — Content Signals.** Added `Content-Signal:` to `robots.txt`.
- [x] **Smaller gaps.** `<time datetime>` on every writing/talk date (machine-readable
      pubdates); fact table in the Now section (engines lift tables wholesale, and it
      targets the check-size question no engine currently answers).

## Deliberately skipped

- **Task 1 — comparison/alternatives page.** Belongs on canonical.cc, not a personal
  bio page. This is the single biggest scorer (+5) and the one question where 4/4
  engines already name the brand — worth doing there.
- **Task 7 — MCP endpoint.** GitHub Pages cannot host one: every MCP transport needs
  JSON-RPC over POST, and Pages returns 405. A descriptor pointing at nothing is
  worse than none (the plan says so). If wanted, host the server as a Cloudflare
  Worker — canonical.cc already runs that pattern — and point a descriptor at it.

## Needs a human decision

- **`ai-train` stance.** `robots.txt` here explicitly *invites* the training crawlers
  by name (CCBot, Google-Extended, Applebot-Extended), so Content-Signal is set to
  `ai-train=yes` to match rather than silently reversing that choice. **This is the
  opposite of canonical.cc, which sets `ai-train=no`.** Pick one and make them agree.
- **Stale README.** README says Pages serves the `master` branch. It serves `main`
  (verified: live bytes == `origin/main`). Left as-is — out of scope for this pass.

## Verification

- All 4 JSON-LD blocks parse; still to paste into <https://validator.schema.org/>.
- HTML re-parsed: no unclosed or mismatched tags.
- `fetch-substack.py` is idempotent against both `index.html` and `index.md` — the
  committed state already equals what the next cron run produces, so no churn.
- Rendered in headless Chrome at desktop and at a 342px content width: eyebrow
  styling unchanged, table wraps without overflow.
